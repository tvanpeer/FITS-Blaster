 1. After Reset switch back to ‘All’ frames.
 2. In Selected mode allow for command A, (command D and I do work)
 3. make a ‘command …’ keyboard shortcut to select all rejected images in the column
 4. Add sort by ‘Rejected’ to the pulldown at the top of the thumbnails
 5. On open folder, use a setting (in the dialog and in Settings) to include ‘REJECTED’ directories and mark those images as rejected.
 6. Changing sub folder scanning in the ‘open folder …’ dialog should change Settings (or ask for it)
 7. Make colour/grey switch iconized (three overlapping colour circles for colour, two b&W circles for grey)
 8. Add ko-fi link in ‘About FITS Blaster’
 9. If I multi select thumbnails, only one dot in the session chart is made bigger, should be all dots associated with selected images.
10. At first open, the first thumbnail is selected, however shift click does not multi select. You first have to select the first image again
11. Move [Score.md](http://Score.md) to public docs and turn into a seperate web page, but first check if the text is still in line with the code
12. Update [release-procedure.md](http://release-procedure.md)
13. Move [raw-support.md](http://raw-support.md) to internal