1. Home page on the web site has ‘drag-to-reject’ and ‘Auto-reject’ instead of ‘drag-to-select’ and ‘Auto-select’
2. Numbers next to folders in thumbnail bar are totals, not the specific number in that view
3. Metrics buttons above session chart work, but do not visually change to ‘pressed’
4. Cmd-Click in Selected view selects or deselects the image instead of removing it from the view
5. At first open, the first thumbnail is selected, however shift click does not multi select. You first have to select the first image again. This bug was fixed in 1.15.1 but has returned