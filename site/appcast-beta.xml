<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>FITS Blaster Beta Updates</title>
        <link>https://astrophoto-app.com</link>
        <description>Beta release notes for FITS Blaster</description>
        <language>en-us</language>
        <item>
            <title>FITS Blaster 1.23-beta.2</title>
            <pubDate>Sun, 12 Apr 2026 15:03:54 +0000</pubDate>
            <sparkle:version>186</sparkle:version>
            <sparkle:shortVersionString>1.23-beta.2</sparkle:shortVersionString>
            <description><![CDATA[
<h2>2026-04-12 — QuickLook previews and thumbnails for all FITS formats</h2>
<h3>Added</h3>
<ul>
  <li>QuickLook preview and thumbnail support for floating-point FITS files (BITPIX -32 and -64). Previously these showed blank icons in Finder.</li>
  <li>QuickLook preview and thumbnail support for 3-channel RGB colour FITS files (NAXIS3=3), such as stacked integrations from Astro Pixel Processor.</li>
  <li>Asinh stretch (beta=50) for float FITS previews — compresses bright cores while revealing faint nebulosity in linear astrophotography data.</li>
  <li>Large-file subsampling: files over 2048px are stride-sampled during preview to stay within QuickLook&#x27;s memory limit (~120 MB), enabling previews of 1+ GB stacked integrations.</li>
  <li>`FITSReader.readForPreview(from:)` — dedicated read path that accepts float BITPIX and multi-channel files without affecting the main app&#x27;s integer-only pipeline.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>Zero-pixel exclusion in percentile estimation prevents stacking artifacts (unfilled corners) from skewing the preview stretch.</li>
</ul>
<p>---</p>
<h2>2026-04-12 — SwiftUI review fixes, accessibility, tests, and rejected-view cursor fix</h2>
<h3>Added</h3>
<ul>
  <li>111 unit and integration tests covering FITSReader, quality scoring, filter group normalisation, frame metrics, Bayer pattern parsing, histogram computation, and star detection with synthetic images (single star FWHM accuracy, multi-star count, gradient background robustness, noise rejection, elongated star eccentricity, FITS round-trip pipeline, CPU image stretch).</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Undo-reject and reject actions now advance the cursor to the next visible entry, matching the flagged-view behaviour.</li>
  <li>VoiceOver: inspector toggle and chart style buttons now have accessible labels instead of empty strings.</li>
  <li>VoiceOver: session chart tap overlay now declares `.isButton` trait.</li>
  <li>VoiceOver: filter strip chips are individually focusable (replaced ScrollView with HStack) and announce selected state.</li>
  <li>VoiceOver: inspector panel exposes children to VoiceOver navigation.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>Settings tabs migrated from deprecated `.tabItem()` to the `Tab` API.</li>
  <li>Filter group header uses cached `GroupStats` instead of recomputing O(n log n) median on every render.</li>
  <li>`.filter { }.count` replaced with `.count(where:)` in ExportPanel and BatchProgressBar.</li>
  <li>Drag handle uses semantic `.quinary`/`.quaternary` colours instead of manual opacity.</li>
  <li>`AppDelegate` annotated `@MainActor` for strict concurrency.</li>
  <li>Unnecessary explicit `return` removed from single-expression switch cases.</li>
  <li>Menu command views extracted from `FitsBlasterApp.swift` into `MenuCommands.swift`.</li>
</ul>
<p>---</p>
<h2>2026-04-09 — Session chart bars, filter fix, play mode, flip, and beta channel</h2>
<h3>Added</h3>
<ul>
  <li>Session chart: optional bar chart mode as alternative to dots. Toggle via the chart header button. Preference is persisted.</li>
  <li>Play mode: press P to auto-advance through images at a configurable speed (0.2–5.0 seconds per frame). A speed slider appears in the toolbar during playback. Manual navigation stops playback automatically.</li>
  <li>Flip 180°: press V to rotate the current image (or batch selection) 180 degrees for display. A rotation indicator appears on the thumbnail. Does not modify FITS files.</li>
  <li>Cursor memory per filter group: switching between filter buttons (R, G, B, etc.) remembers and restores the selected image for each group.</li>
  <li>Beta release channel: tag a version as `v1.23-beta.1` to publish a test build without updating the homepage. Beta builds get their own page (`beta.html`), appcast feed (`appcast-beta.xml`), and are marked as pre-releases on GitHub.</li>
  <li>Beta page on the website with download link and release notes for the latest beta build.</li>
  <li>Release &amp; issue workflow guide (`guide-releases.html`) documenting the tag-based release process and GitHub Issues workflow.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Filter buttons above the thumbnail sidebar now work correctly in multi-folder sessions.</li>
  <li>Switching filter group or rejection visibility now auto-selects the first visible entry when the cursor is hidden.</li>
  <li>Unflagging an image in the Flagged view now advances the cursor to the next flagged image instead of losing the selection.</li>
  <li>Playback stops automatically when switching filter group or rejection visibility.</li>
  <li>Playback timer moved out of SwiftUI view modifier to prevent task cancellation during view rebuilds.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>O(n²) search in image removal replaced with O(n) scan for selecting the next entry.</li>
  <li>GitHub Actions pipeline detects beta tags and adjusts behaviour: marks releases as pre-release, writes to separate appcast and site files.</li>
  <li>Site navigation now includes a Beta link on all pages.</li>
</ul>
<p>---</p>
<p>**[Full changelog](https://astrophoto-app.com/changelog.html)**</p>
            ]]></description>
            <enclosure
                url="https://github.com/tvanpeer/FITS-Blaster/releases/download/v1.23-beta.2/FITS-Blaster-1.23-beta.2.dmg"
                length="8400770"
                type="application/octet-stream"
                sparkle:edSignature="/+1VXR39jvrwXsFgh5wTDzm5eMNqJIwUUjSFlIEoqgcmJOsIVKAjOCLFThN6BzSR8p389HMKZHSi/f318x3jBA==" />
        </item>
        <item>
            <title>FITS Blaster 1.23-beta.1</title>
            <pubDate>Thu, 09 Apr 2026 18:36:47 +0000</pubDate>
            <sparkle:version>174</sparkle:version>
            <sparkle:shortVersionString>1.23-beta.1</sparkle:shortVersionString>
            <description><![CDATA[
<h2>2026-04-09 — Session chart bars, filter fix, play mode, flip, and beta channel</h2>
<h3>Added</h3>
<ul>
  <li>Session chart: optional bar chart mode as alternative to dots. Toggle via the chart header button. Preference is persisted.</li>
  <li>Play mode: press P to auto-advance through images at a configurable speed (0.2–5.0 seconds per frame). A speed slider appears in the toolbar during playback. Manual navigation stops playback automatically.</li>
  <li>Flip 180°: press V to rotate the current image (or batch selection) 180 degrees for display. A rotation indicator appears on the thumbnail. Does not modify FITS files.</li>
  <li>Beta release channel: tag a version as `v1.23-beta.1` to publish a test build without updating the homepage. Beta builds get their own page (`beta.html`), appcast feed (`appcast-beta.xml`), and are marked as pre-releases on GitHub.</li>
  <li>Beta page on the website with download link and release notes for the latest beta build.</li>
  <li>Release &amp; issue workflow guide (`guide-releases.html`) documenting the tag-based release process and GitHub Issues workflow.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Filter buttons above the thumbnail sidebar now work correctly in multi-folder sessions.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>GitHub Actions pipeline detects beta tags and adjusts behaviour: marks releases as pre-release, writes to separate appcast and site files.</li>
  <li>Site navigation now includes a Beta link on all pages.</li>
</ul>
<p>---</p>
<h2>2026-04-08 — Sparkle auto-update</h2>
<h3>Added</h3>
<ul>
  <li>Automatic update checking via the Sparkle framework. FITS Blaster now checks for new versions once every 24 hours in the background.</li>
  <li>&quot;Check for Updates...&quot; menu item in the FITS Blaster menu for manual update checks.</li>
  <li>Updates are downloaded, installed, and relaunched automatically — no manual DMG drag required.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>FAQ updated: &quot;Does FITS Blaster require an internet connection?&quot; now mentions the lightweight update check.</li>
</ul>
<p>---</p>
<h2>2026-04-08 — Image viewer controls</h2>
<h3>Added</h3>
<ul>
  <li>Zoom slider (0.25×–4×) with live readout; trackpad pinch-to-zoom also supported.</li>
  <li>Brightness slider for quick exposure adjustment without re-rendering.</li>
  <li>Stretch slider to boost or reduce image contrast.</li>
  <li>&quot;Defaults&quot; button to reset all three adjustments in one click.</li>
  <li>Scrollbars appear automatically when the image is zoomed beyond the viewport.</li>
  <li>Viewport indicator: a yellow box on the sidebar thumbnail shows which part of the image is currently visible when zoomed in.</li>
</ul>
<h3>Improved</h3>
<ul>
  <li>Scroll position is now preserved when navigating between frames at the same zoom level.</li>
  <li>Zooming via the slider keeps the viewport centre fixed instead of jumping to the top-left.</li>
  <li>Zoom level persists across app launches; brightness and stretch reset when opening a new folder.</li>
  <li>Swift 6 concurrency fixes: key-event monitor, `PanelAccessoryHelper`, `FolderTracker.init`, concurrent `self` capture, and unused weak-capture removals.</li>
  <li>QL extension `CFBundleVersion` now tracks the main app version automatically via `$(CURRENT_PROJECT_VERSION)`.</li>
</ul>
<p>---</p>
<p>**[Full changelog](https://astrophoto-app.com/changelog.html)**</p>
            ]]></description>
            <enclosure
                url="https://github.com/tvanpeer/FITS-Blaster/releases/download/v1.23-beta.1/FITS-Blaster-1.23-beta.1.dmg"
                length="8361916"
                type="application/octet-stream"
                sparkle:edSignature="lZ0G39L5kEM9l6N1cilzv/QIQ+RwmooFBsv7/skVAGGycm+p/m63Bsi5c9qhHT1qC4z/YiqTw3UapKYaiQ1GBg==" />
        </item>
    </channel>
</rss>
